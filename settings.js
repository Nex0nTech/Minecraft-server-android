// SETTINGS GLOBALI DEL SITO
const NexonSettings = {
  legacyMode: false,        // Usa i file old
  theme: "dark",            // "dark" | "light"
  accent: "green",          // "red" | "yellow" | "green" | "purple"
  showClock: true,          // Mostra data/ora in home
  aiSummary: true,          // Attiva funzione "riassumi con AI"
  accountLinked: false      // Ricorda accesso utente
};
